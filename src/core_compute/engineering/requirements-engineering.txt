# Optional dependencies for engineering scripts (metasurface routing + inverse design).
# Core repo only needs: numpy (see ../requirements.txt).

numpy>=1.20.0

# QUBO routing (routing_qubo_qaoa.py) — Qiskit 2.x + qiskit-optimization 0.7
# OpenQASM 3.0 parsing (qasm_loader.py) — qiskit-qasm3-import
qiskit>=2.0
qiskit-qasm3-import>=0.5.0
qiskit-optimization>=0.7.0
hashable-list>=0.2.0
ordered-set>=4.0.0

# Optional: run QAOA on real IBM Quantum hardware (python routing_qubo_qaoa.py --hardware)
# qiskit-ibm-runtime

# Inverse design (metasurface_inverse_net.py)
torch>=1.13.0

# Optional: meta-atom simulation (squid_spectrum_scqubits.py)
# scqubits>=3.0

# Optional: open quantum systems / TLS decoherence (decoherence_rates.py, routing --use-qutip-decoherence)
# qutip>=4.7

# Topology visualizer (viz_topology.py)
matplotlib>=3.5.0

# Optional: 2D London equation / Meissner screening (superscreen_demo.py, run_pipeline --with-superscreen)
# superscreen>=0.12

# Optional: FDTD S-parameter dataset for forward prediction net (meep_s_param_dataset.py)
# pymeep  # or install via conda: conda install -c conda-forge pymeep

# HEaC (Hardware-Engineering-as-Code): phase-to-geometry, meta-atom library, manifest
scipy>=1.7.0
# Optional: Meep for real unit-cell sweep (engineering/heac/meep_unit_cell_sweep.py); else synthetic library
# pymeep
# Optional: GDSII export from geometry manifest (engineering/heac/manifest_to_gds.py)
# gdsfactory
